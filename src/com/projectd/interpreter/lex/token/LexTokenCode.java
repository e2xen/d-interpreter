package com.projectd.interpreter.lex.token;

public enum LexTokenCode {
    IDENTIFIER,
    LITERAL,

    IF,
    THEN,
    ELSE,
    END,
    VAR,
    WHILE,
    LOOP,
    FOR,
    IN,
    PRINT,
    OR,
    AND,
    XOR,
    // TODO: list all
}
